#include <stdio.h>

/**
* main - Short prints line using printf
* Return: null
*/
int main(void)
{
	char str[] = "with proper grammar, but the outcome is a piece of art,\n";

	printf("%s", str);
	return (0);
}
